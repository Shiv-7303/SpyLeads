{
  "name": "extension",
  "version": "1.0.0",
  "description": "1. Go to `chrome://extensions` in your browser. 2. Enable Developer Mode. 3. Click \"Load unpacked\" and select the `extension/` folder.",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "tailwindcss": "^3.4.3"
  }
}
